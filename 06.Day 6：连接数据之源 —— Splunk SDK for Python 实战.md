
# 🚀 Day 6：连接数据之源 —— Splunk SDK for Python 实战

**今日目标**：直接在 Add-on Builder (AOB) 强大的前端代码编辑器中，利用 Splunk SDK for Python 获取 `session_key`。动态读取用户配置的 Index，执行防超载的随机抽样 SPL，拉取 5 条真实的底层日志。在 AOB 内完成一键测试后，回到 Splunk 搜索栏进行最终的后台调度验证。

**🧠 极客复盘：为什么需要随机拉取？**
在 PEAK 威胁狩猎框架中，AI 扮演的是“安全分析师”。分析师不能凭空捏造假设，必须基于当前环境的真实数据基线（Baseline）。我们通过动态锁定数据源并进行随机抽样，确保 AI 每次巡逻都能看到不同切面的系统状态，避免思维固化。

---

### 🛠️ Step 1：回到 AOB 的代码实验室

我们今天全程在 Splunk 前端网页中操作。

1. 在浏览器中打开 Splunk (`http://localhost:8000`)。
2. 进入 **Splunk Add-on Builder**，点击进入你的 **PEAK-llm-analyzer** 项目。
3. 点击顶部导航栏的 **Configure Data Collection (配置数据收集)**。
4. 在列表中找到我们在 Day 4 创建的 `PEAK AI Hunter`，点击它右侧的 **Edit (编辑)**。
5. 此时会弹出向导，直接点击顶部的第三个标签页：**Define & Test (定义与测试)**。

你现在看到的就是 AOB 自带的 IDE（集成开发环境）。左侧是参数输入区，右侧是代码编辑区。

---

### 💻 Step 2：在 AOB 中注入 SDK 抽样逻辑

在右侧的 **Code Editor** 中，我们需要做两处修改。

**第一处：在文件最顶部导入 SDK**
滚动到代码的第一行，在原有的 `import` 列表下方，加入 Splunk SDK：
```python
import os
import sys
import time
import datetime
import json
# NEW: Import Splunk SDK bindings
import splunklib.client as client
import splunklib.results as results
```

**第二处：重写 collect_events 函数**
向下滚动找到 `def collect_events(helper, ew):`。将其下方的代码完全替换为以下逻辑：

```python
def collect_events(helper, ew):
    """Implement your data collection logic here"""
    
    helper.log_info("PEAK AI Hunter started data collection cycle.")

    try:
        # 1. Acquire the session_key from the modular input context
        session_key = helper.context.meta.get("session_key")
        
        if not session_key:
            helper.log_error("Failed to acquire session_key from context.")
            return

        # 2. Connect to the Splunk service using the SDK
        service = client.Service(token=session_key)

        # 3. Dynamically hijack the user-selected Index
        target_index = helper.get_output_index()
        if not target_index:
            target_index = "main" # Fallback index for testing

        # 4. Construct the randomized baseline SPL
        # Geek Tip: We use `head 10000` BEFORE `random()` to prevent timeouts.
        search_query = (
            f"search index={target_index} "
            f"| head 10000 "
            f"| eval rand_val=random() "
            f"| sort 5 rand_val"
        )
        
        # 5. Use jobs.oneshot for fast, synchronous retrieval in JSON format
        kwargs_oneshot = {"output_mode": "json"}
        helper.log_info(f"Executing randomized oneshot SPL: {search_query}")
        
        oneshot_search_results = service.jobs.oneshot(search_query, **kwargs_oneshot)

        # 6. Parse the results
        reader = results.JSONResultsReader(oneshot_search_results)
        
        baseline_logs = []
        for result in reader:
            if isinstance(result, dict) and "_raw" in result:
                baseline_logs.append(result["_raw"])

        # 7. Log the extracted data length to verify success
        if len(baseline_logs) > 0:
            helper.log_info(f"Successfully fetched {len(baseline_logs)} random raw logs from index '{target_index}'.")
            helper.log_info(f"Sample Random Log [1]: {baseline_logs[0][:200]}...")
        else:
            helper.log_warning(f"SPL executed successfully, but returned 0 events from index '{target_index}'.")

    except Exception as e:
        helper.log_error(f"Execution error occurred during Splunk SDK search: {str(e)}")
```

---

### 🔍 Step 3：一键测试与保存 (AOB 的终极优势)

这就是在 AOB 中开发的爽感所在，你不需要等后台调度器，直接可以看结果！

1. 在页面左侧的 **Data input parameters** 区域，确保有一个用于测试的 Index 名称（比如写 `main` 或 `_internal`）。
2. 点击页面右上角绿色的 **Test** 按钮。
3. 向下滚动查看页面底部的 **Output (输出)** 面板。

如果看到了绿色的 INFO 级别日志：`Executing randomized oneshot SPL...` 并且紧跟着 `Successfully fetched 5 random raw logs...`，**这证明你的 Python 代码逻辑完美跑通了！**

4. 测试通过后，点击右上角的 **Save** 按钮保存代码，并关闭向导。

---

### 🎯 Step 4：全局验证 —— 回到 Splunk 确认后台调度

虽然我们在 AOB 里手动测试成功了，但别忘了在 Day 5 中，我们已经配置了这个任务每 60 秒在后台自动运行一次。保存代码后，这个全新的随机抽样逻辑就已经被底层调度器接管了！

1. 点击左上角的 `Splunk>enterprise` Logo，回到 Splunk 首页，进入 **Search & Reporting** 应用。
2. 我们需要去 Splunk 的底层内部日志（splunkd.log）中查看后台任务的运行轨迹。在搜索框中输入：
   ```spl
   index=_internal sourcetype="splunkd" "PEAK AI Hunter started data collection cycle" OR "Sample Random Log"
   ```
3. 将时间范围调整为 **Last 15 minutes**，点击搜索。

**🎉 终极成功标志：**
你会看到日志不是只有你刚才手动 Test 的那一次，而是**每隔 60 秒**就会稳定地输出一次 `Successfully fetched 5 random raw logs...`。这证明你写的代码不仅没毛病，而且正在源源不断地为 AI 采集真实的随机样本！

明天（Day 7），我们将把这些动态抽取的真实随机日志，配合极其严酷的 System Prompt，正式发往大模型，正式发往大模型，让 AI 化身资深安全专家。它将观察这些正常的系统日志，并像黑客一样逆向思考，推测出系统可能遭受的攻击方式（进入威胁狩猎模式）！
