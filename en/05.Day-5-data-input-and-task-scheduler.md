

---
# 🚀 Day 5: 任务调度器 —— 建立后台数据输入流与“心跳”机制 (Data Inputs)

**今日目标**: 理解 Data Input（数据输入）的底层调度原理。我们将顺应 AOB 的强制参数规则配置输入界面，编写 Python 探针代码，并在前端真实启动任务。最终，我们要去 Splunk 的内部索引（`_internal`）中，亲眼见证代码被系统定时“唤醒”的运行轨迹。

---

### 🧠 极客白话：什么是 Data Input 与“心跳”机制？

对于初学者来说，Data Input（数据输入）听起来像是一个让你填表单的界面。但实际上，它是 Splunk 插件的**“心脏起搏器”**！

* **频率 (Interval) = 心率**: 当你在界面上设置了 Interval 为 60 秒时，相当于给系统设定了一个定时闹钟。
* **执行 (Execution) = 心跳**: 每隔 60 秒，Splunk 的底层调度器就会自动去唤醒并执行一次你写的 Python 脚本，就像心脏跳动一样。
* **多实例 (Multi-instance)**: 你可以基于同一个 Python 脚本，在前端创建无数个任务（比如任务 A 每 60 秒查防火墙，任务 B 每 10 分钟查数据库）。它们独立跳动，互不干扰。

今天，我们要给这颗心脏装上探针，记录下它的每一次跳动！

---

### 🛠️ Step 1: 配置前端 UI (化被动为主动)

AOB 有一个硬性的系统规定：**数据输入模块必须至少保留一个自定义参数**，否则无法保存并进入下一步。既然我们不能把它删光，那就把它改造成一个有用的“探针字段”。

**详细操作路径：**
1. **登录 Splunk**: 打开浏览器，输入你的 Splunk 地址（通常是 `http://localhost:8000`），输入管理员账号密码登录。
2. **进入 AOB 应用**: 在 Splunk 首页左侧的 Apps（应用）列表中，找到并点击 **Splunk Add-on Builder**。
3. **打开项目**: 在 AOB 首页的“Created with Add-on Builder”列表中，找到我们之前创建的 **PEAK-llm-analyzer** 项目，点击它进入项目主页。
4. **进入数据收集配置页**: 在项目主页的顶部导航栏，点击 **Configure Data Collection**（配置数据收集）。
5. **编辑任务模板**: 在弹出的列表中，你会看到我们在 Day 4 创建的 `PEAK AI Hunter`。点击这行最右侧的 **Edit**（编辑）按钮，此时会弹出一个全屏向导窗口。
6. **核对基础属性 (Tab 1)**:
   * 此时你处于向导的第一个标签页 **Data Input Properties**（数据输入属性）。
   * 滚动到页面最底部，找到 **Collection interval (seconds)**，确保这里填的是 `60`（这是我们默认的 60 秒心率）。
7. **改造参数表单 (Tab 2)**:
   * 点击页面顶部的第二个标签页：**Data Input Parameters**（数据输入参数）。
   * 在页面的中间画布区，你会看到 AOB 自动为你生成了一个无法删除的“Text Input（文本输入框）”。
   * **鼠标左键点击选中**这个输入框。
   * 看向页面的**右侧面板 (Property Editor)**，在这里修改它的核心属性：
     * **Internal Name** (系统内部调用名): 改为 `custom_message`
     * **Label** (展示给用户看的标签): 改为 `Custom Heartbeat Message`
     * **Default value** (默认值): 填入 `Alive`
8. **进入下一关**: 属性修改完毕后，点击页面右上角（或右下角）的绿色 **Next >** 按钮，系统会保存配置并带你进入核心的代码编写页面。

---

### 💻 Step 2: 注入“心跳探针”代码 (Define & Test 页面)

此时，你已经进入了向导的第三个标签页：**Define & Test**（定义与测试）。这个页面的布局是：**左侧**显示你刚才配置的参数表单（用于模拟输入），**右侧**是 AOB 自带的 **Code Editor**（代码编辑器）。

**详细操作路径：**
1. **定位核心函数**: 在右侧的代码编辑器中向下滚动，找到这句代码：
   ```python
   def collect_events(helper, ew):
   ```
2. **清理默认代码**: 将 `def collect_events(helper, ew):` 这一行**下方**的所有默认代码全部删除（只保留上面那句 `def` 和它的参数）。
3. **注入探针代码**: 将以下经过严格验证的探针代码，完整粘贴到 `def collect_events(helper, ew):` 下方（注意保持 Python 的缩进规范）。
**⚠️ 极客铁律**: 代码注释与日志严格使用纯英文。

```python
def collect_events(helper, ew):
    """Implement your data collection logic here"""
    import time
    import datetime
    
    try:
        # 1. Get the current execution time
        current_time = datetime.datetime.now().strftime("%Y-%m-%d %H:%M:%S")
        
        # 2. Get the specific instance name (Stanza) triggered by the scheduler
        stanza_name = helper.get_input_stanza_names()
        
        # 3. Get the target index configured by the user
        # Note: During the AOB "Test" click, this might return None. In real runs, it returns the Index.
        target_index = helper.get_output_index() or "default_test_index"
        
        # 4. Read the custom parameter (CRITICAL: MUST use get_arg for Data Inputs)
        custom_msg = helper.get_arg("custom_message")
        
        # 5. Construct the heartbeat log payload
        log_msg = f"[PEAK AI Hunter Heartbeat] Time: {current_time} | Task: {stanza_name} | Target Index: {target_index} | Custom Msg: {custom_msg} | Status: Scheduler awakened the Python script."
        
        # 6. Write this heartbeat log to Splunk's _internal index (splunkd.log)
        helper.log_info(log_msg)
        
    except Exception as e:
        helper.log_error(f"Heartbeat execution error: {str(e)}")
```

4. **执行防崩测试 (Test)**:
   * 看向页面左侧的参数模拟区，在 **Custom Heartbeat Message** 文本框里随便敲几个字（比如输入 `Test Run`）。
   * 点击页面**右上角**的绿色 **Test** 按钮。
   * 立即向下滚动网页，查看编辑器底部的 **Output (输出)** 面板。只要这里没有弹出大段的红色报错，就说明你的代码语法完美无缺！
5. **固化代码 (Save) —— 极其重要**: 测试没报错后，**必须**点击右上角绿色的 **Save** 按钮！只有点了 Save，这段代码才会被真正写入到底层的 Python 文件中。看到保存成功的提示后，关闭向导窗口。

---

### 🔍 Step 3: 在前端创建任务，检索真实的“心跳”

代码写好并保存后，我们需要像真实用户一样，去前端创建一个“任务实例”来激活底层的调度器。

1. 点击左上角的 `Splunk>enterprise` 徽标返回 Splunk 首页。
2. 在左侧进入我们的应用：**PEAK-llm-analyzer**。
3. 点击顶部导航栏的 **Inputs (输入)** 标签页。
4. 点击右上角的 **Create New Input** -> **PEAK AI Hunter**。
5. 填入真实的任务参数：
   * **Name**: `my_first_heartbeat`
   * **Interval**: `60`
   * **Index**: `main`
   * **Custom Heartbeat Message**: `Engine Started`
6. 点击 **Add** 保存。此时，你的任务已经脱离了手动测试，正式被系统调度器接管！

**🎉 见证奇迹的时刻（搜索日志）：**
等待一到两分钟，让调度器在后台跑完几轮。点击顶部的 **Search (搜索)** 标签页，在搜索框中输入：
```spl
index=_internal "[PEAK AI Hunter Heartbeat]"
```
将时间范围调整为 **Last 15 minutes**，点击搜索。

你会看到类似于下面这样的日志，**精准地每隔 60 秒出现一次**：
> `... [PEAK AI Hunter Heartbeat] Time: 2026-03-26 21:46:24 | Task: my_first_heartbeat | Target Index: main | Custom Msg: Engine Started | Status: Scheduler awakened the Python script.`

**恭喜你！** 你现在已经彻底搞懂了 AOB 数据输入的底层原理。明天（Day 6），我们将在这段定时醒来的代码中加入 Splunk SDK，让它去抓取真实的业务日志！